[package]
name = "settings-store"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
multizen-core = { path = "../multizen-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
